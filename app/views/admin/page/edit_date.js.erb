$("#pop_application").html("<%= escape_javascript(render :partial => "/admin/page/edit_date") %>");
var theTop = parseInt($('#memScrollTop').attr('viewMiddleTop')) ;

easyDialog.open({
  container : 'pop_application',
  drag : true,
  //lock : false,
  fixed : true,
  overlay : true,
  follow : 'memScrollTop',
  followX : 141,
  followY : theTop 
});

createShadow();

var content = $("#<%= @id%>_content");
content.find(".newlet_text").val($("#<%= @id%>_title").val());