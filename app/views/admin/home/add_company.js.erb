var theTop = parseInt($('#memScrollTop').attr('viewMiddleTop')) ;

easyDialog.open({
	container : 'pop_company',
	drag : true,
	//lock : false,
	fixed : true,
	overlay : true,
	follow : 'memScrollTop',
	  followX : 0,
	  followY : theTop,
});	

createShadow();
$("#pop_company").html("<%= escape_javascript( render :partial => "/admin/home/add_company_info") %>");