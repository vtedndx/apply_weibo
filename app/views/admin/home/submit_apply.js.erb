if($(".no_data").html() == undefined){
	$(".Box_table_tr").after("<%= escape_javascript( render :partial => "/admin/home/one_apply", :locals => {:apply => @apply}) %>");
}else {
	$(".no_data").detach();
	$(".adminBox_bot_ft").detach();
	$(".adminBox_bot_btn").detach();
	$(".Box_table_tr").after("<%= escape_javascript( render :partial => "/admin/home/one_apply", :locals => {:apply => @apply}) %>");
}
close_easy_dialog();

var Box_table_trb=$('.Box_table_trb');
  Box_table_trb.each(function(index, element) {
        var Box_table_trb_td=$(this).find('td:eq(1)');
    Box_table_trb_td.css({'text-align':'left','padding-left':'10px'});
    Box_table_trb_td.text(subString(Box_table_trb_td.text(),40))
    });
function subString(str, len) { 
var newLength = 0; 
var newStr = ""; 
var chineseRegex = /[^\x00-\xff]/g; 
var singleChar = ""; 
var strLength = str.replace(chineseRegex,"**").length; 
for(var i = 0;i < strLength;i++) { 
singleChar = str.charAt(i).toString(); 
if(singleChar.match(chineseRegex) != null) { 
newLength += 2; 
}else { 
newLength++; 
} 
if(newLength > len) { 
break; 
} 
newStr += singleChar; 
} 
if(strLength > len) { 
newStr += "..."; 
} 
return newStr; 
}